import { CardIconControllerDirective } from './card-icon-controller.directive';

describe('CardIconControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new CardIconControllerDirective();
    expect(directive).toBeTruthy();
  });
});
